---
description: Generate an image with OpenAI gpt-image-2 (skip the model-choice prompt).
---

The user has chosen GPT (`gpt-image-2`) as the image model. Follow the
`image-gen` skill end-to-end with `model: "gpt"` on every call to
`generate_image`. **Do not ask the user which model to use** — they've
already chosen by invoking `/image-gpt`.

The user's request: $ARGUMENTS
